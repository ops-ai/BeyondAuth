apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-docs-stg
  labels:
    app: ba-docs-stg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-docs-stg
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: ba-docs-stg
    spec:
      containers:
      - name: ba-docs-stg
        image: opsairegistry.azurecr.io/beyondauth-docs:latest 
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: VaultUri
          valueFrom:
           secretKeyRef:
             name: "ba-docs-secrets"
             key: "VaultUri"
        - name: TenantId
          valueFrom:
           secretKeyRef:
             name: "ba-docs-secrets"
             key: "TenantId"
        - name: ClientId
          valueFrom:
           secretKeyRef:
             name: "ba-docs-secrets"
             key: "ClientId"
        - name: ClientSecret
          valueFrom:
           secretKeyRef:
             name: "ba-docs-secrets"
             key: "ClientSecret"
        ports:
        - containerPort: 443
          name: docs-stg-https
        - containerPort: 80
          name: docs-stg-http
        livenessProbe:
          httpGet:
            path: /hz
            port: docs-stg-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 5
          periodSeconds: 20
          timeoutSeconds: 4
        startupProbe:
          httpGet:
            path: /hz
            port: docs-stg-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 5
          periodSeconds: 60
          timeoutSeconds: 4
---
apiVersion: v1
kind: Service
metadata:
  name: ba-docs-stg-srv
spec:
  type: ClusterIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 443
    name: docs-stg-https
  - port: 80
    protocol: TCP
    targetPort: 80
    name: docs-stg-http
  selector:
    app: ba-docs-stg