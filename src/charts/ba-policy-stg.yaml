apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-policy-stg
  labels:
    app: ba-policy-stg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-policy-stg
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: ba-policy-stg
    spec:
      containers:
      - name: ba-policy-stg
        image: opsairegistry.azurecr.io/beyondauthweb:latest 
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: VaultUri
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "VaultUri"
        - name: TenantId
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "TenantId"
        - name: ClientId
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "ClientId"
        - name: ClientSecret
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "ClientSecret"
        ports:
        - containerPort: 443
          name: poli-stg-https
        - containerPort: 80
          name: poli-stg-http
        livenessProbe:
          httpGet:
            path: /hz
            port: poli-stg-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
        startupProbe:
          httpGet:
            path: /hz
            port: poli-stg-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ba-policy-stg-srv
spec:
  type: ClusterIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 443
    name: poli-stg-https
  - port: 80
    protocol: TCP
    targetPort: 80
    name: poli-stg-http
  selector:
    app: ba-policy-stg