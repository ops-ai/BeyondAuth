apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-audit
  labels:
    app: ba-audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-audit
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: ba-audit
    spec:
      containers:
      - name: ba-audit
        image: opsairegistry.azurecr.io/beyondauthweb:latest 
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: VaultUri
          valueFrom:
           secretKeyRef:
             name: "ba-audit-secrets"
             key: "VaultUri"
        - name: TenantId
          valueFrom:
           secretKeyRef:
             name: "ba-audit-secrets"
             key: "TenantId"
        - name: ClientId
          valueFrom:
           secretKeyRef:
             name: "ba-audit-secrets"
             key: "ClientId"
        - name: ClientSecret
          valueFrom:
           secretKeyRef:
             name: "ba-audit-secrets"
             key: "ClientSecret"
        ports:
        - containerPort: 443
          name: audit-https
        - containerPort: 80
          name: audit-http
        livenessProbe:
          httpGet:
            path: /hz
            port: audit-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
        startupProbe:
          httpGet:
            path: /hz
            port: audit-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ba-audit-srv
spec:
  type: ClusterIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 443
    name: audit-https
  - port: 80
    protocol: TCP
    targetPort: 80
    name: audit-http
  selector:
    app: ba-audit