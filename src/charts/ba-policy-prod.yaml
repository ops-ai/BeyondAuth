apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-policy
  labels:
    app: ba-policy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-policy
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: ba-policy
    spec:
      containers:
      - name: ba-policy
        image: opsairegistry.azurecr.io/beyondauth-policy:latest 
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Staging"
        - name: VaultUri
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "VaultUri"
        - name: TenantId
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "TenantId"
        - name: ClientId
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "ClientId"
        - name: ClientSecret
          valueFrom:
           secretKeyRef:
             name: "ba-policy-secrets"
             key: "ClientSecret"
        ports:
        - containerPort: 443
          name: policy-https
        - containerPort: 80
          name: policy-http
        livenessProbe:
          httpGet:
            path: /hz
            port: policy-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
        startupProbe:
          httpGet:
            path: /hz
            port: policy-http
            httpHeaders:
              - name: Accept
                value: text/html
          failureThreshold: 1
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ba-policy-srv
spec:
  type: ClusterIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 443
    name: policy-https
  - port: 80
    protocol: TCP
    targetPort: 80
    name: policy-http
  selector:
    app: ba-policy